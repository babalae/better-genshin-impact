<UserControl x:Class="BetterGenshinImpact.View.Pages.ScriptControlPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dd="urn:gong-wpf-dragdrop"
             xmlns:local="clr-namespace:BetterGenshinImpact.View.Pages"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:helpers="clr-namespace:BetterGenshinImpact.Helpers"
             xmlns:pages="clr-namespace:BetterGenshinImpact.ViewModel.Pages"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             d:DataContext="{d:DesignInstance Type=pages:ScriptControlViewModel}"
             d:DesignHeight="850"
             d:DesignWidth="800"
             ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
             ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             FontFamily="{StaticResource TextThemeFontFamily}"
             Foreground="{DynamicResource TextFillColorPrimaryBrush}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/View/Controls/Style/ListViewEx.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="8,0,8,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  左侧栏  -->
        <Grid Grid.Column="0" Margin="0,8,8,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Border Grid.Row="0"
                        BorderBrush="{DynamicResource ControlStrongFillColorDisabledBrush}"
                        BorderThickness="0,0,0,1">
                    <ui:TextBlock Margin="4,4,8,4"
                                  FontTypography="BodyStrong"
                                  Text="{Binding [Script_001_22f6f8], Source={x:Static helpers:Lang.S}}"
                                  TextAlignment="Center" />
                </Border>
                <ui:ListView Grid.Row="1"
                             dd:DragDrop.IsDragSource="True"
                             dd:DragDrop.IsDropTarget="True"
                             dd:DragDrop.UseDefaultDragAdorner="True"
                             ItemsSource="{Binding ScriptGroups, Mode=TwoWay}"
                             SelectedItem="{Binding SelectedScriptGroup, Mode=TwoWay}"
                             SelectionMode="Single">
                    <ui:ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Margin="8,4" Text="{Binding Name, Mode=OneWay}" >
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding NextFlag}" Value="True">
                                                <Setter Property="Foreground" Value="Green" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                              </TextBlock>
                        </DataTemplate>
                    </ui:ListView.ItemTemplate>
                    <ui:ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Command="{Binding AddScriptGroupCommand}" Header="{Binding [Script_002_99e704], Source={x:Static helpers:Lang.S}}" />
                            <MenuItem Command="{Binding DeleteScriptGroupCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                      Header="{Binding [Script_003_c73ef1], Source={x:Static helpers:Lang.S}}" />
                            <MenuItem Command="{Binding RenameScriptGroupCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                      Header="{Binding [Script_004_c8ce4b], Source={x:Static helpers:Lang.S}}" />
                            <MenuItem Command="{Binding CopyScriptGroupCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                      Header="{Binding [Script_005_4c2b3e], Source={x:Static helpers:Lang.S}}" />
                            <MenuItem Command="{Binding AddScriptGroupNextFlagCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                      Header="{Binding [Script_006_3634f9], Source={x:Static helpers:Lang.S}}" />
                        </ContextMenu>
                    </ui:ListView.ContextMenu>
                </ui:ListView>
                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ui:Button Grid.Row="0"
                               Margin="2"
                               HorizontalAlignment="Stretch"
                               Command="{Binding StartMultiScriptGroupCommand}"
                               Content="{Binding [Btn_LoopExec], Source={x:Static helpers:Lang.S}}" />

                    <ui:Button Grid.Row="1"
                               Margin="2"
                               HorizontalAlignment="Stretch"
                               Command="{Binding ContinueMultiScriptGroupCommand}"
                               Content="{Binding [Btn_ContinueExec], Source={x:Static helpers:Lang.S}}" />
                </Grid>
            </Grid>
        </Grid>

        <!--  分割  -->
        <ui:FontIcon Grid.Column="1"
                     HorizontalAlignment="Center" Glyph="&#xf7a5;" Style="{StaticResource FaFontIconStyle}" />
        <GridSplitter Grid.Column="1"
                      Width="12"
                      HorizontalAlignment="Stretch"
                      Background="{DynamicResource ControlStrongFillColorDisabledBrush}"
                      Focusable="False"
                      Opacity="0.4" />

        <!--  右侧栏:无选中组  -->
        <Border Grid.Column="2"
                Margin="6"
                Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1,1,1,1"
                CornerRadius="8">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger
                            Binding="{Binding SelectedScriptGroup, Converter={StaticResource NotNullConverter}}"
                            Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid Margin="8,8,0,0">

                <StackPanel>
                    <ui:TextBlock Margin="0,0,0,8"
                                  FontTypography="BodyStrong"
                                  Text="{Binding [Script_007_3c0095], Source={x:Static helpers:Lang.S}}" />
                    <ui:TextBlock Margin="0,0,0,8"
                                  Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                                  TextWrapping="Wrap">
                        <Run Text="{Binding [Script_531_209085], Source={x:Static helpers:Lang.S}, Mode=OneWay}"/>
                        <Hyperlink Command="{Binding GoToScriptGroupUrlCommand}"
                                   Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="{Binding [Script_532_8710e4], Source={x:Static helpers:Lang.S}, Mode=OneWay}"/>
                        </Hyperlink>
                    </ui:TextBlock>
                    <ui:TextBlock Margin="0,0,0,8"
                                  FontTypography="BodyStrong"
                                  Text="{Binding [Script_008_fd1219], Source={x:Static helpers:Lang.S}}" />
                    <ui:Button Margin="0,0,0,12"
                               Command="{Binding AddScriptGroupCommand}"
                               Content="{Binding [Btn_AddConfig], Source={x:Static helpers:Lang.S}}" />
                    <!--  示例配置组  -->
                    <!--<ui:TextBlock Margin="0,0,0,8"
                                  FontTypography="BodyStrong"
                                  Text="也可以直接点击导入下面的示例配置组" />
                    <ui:TextBlock Margin="0,0,0,8"
                                  Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                                  TextWrapping="Wrap">
                        <Hyperlink Command="{Binding ImportScriptGroupCommand}"
                                   CommandParameter="AutoCrystalflyExampleGroup"
                                   Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}">
                            导入自动晶蝶示例配置组
                        </Hyperlink>
                    </ui:TextBlock>-->
                </StackPanel>
            </Grid>
        </Border>

        <!--  右侧栏:选中组  -->
        <Border Grid.Column="2"
                Margin="6"
                Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1,1,1,1"
                CornerRadius="8">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedScriptGroup}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid Margin="8,8,0,0">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <ui:TextBlock Margin="0,0,0,8"
                                  FontTypography="BodyStrong"
                                  Text="{Binding [Script_009_f801df], Source={x:Static helpers:Lang.S}}" />
                    <ui:TextBlock Margin="0,0,0,8"
                                  FontTypography="BodyStrong"
                                  Text="{Binding SelectedScriptGroup.Name}" />
                </StackPanel>

                <ui:TextBlock Grid.Row="1"
                              Margin="0,0,0,8"
                              Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                              TextWrapping="Wrap">
                    <Run Text="{Binding [Script_533_c3a64f], Source={x:Static helpers:Lang.S}, Mode=OneWay}"/>
                </ui:TextBlock>


                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <ui:Button Command="{Binding StartScriptGroupCommand}"
                               Content="{Binding [Btn_Run], Source={x:Static helpers:Lang.S}}"
                               Icon="{ui:SymbolIcon Play24}" />
                    <Separator Width="10" Opacity="0" />


                    <ui:DropDownButton Content="{Binding [Btn_Add], Source={x:Static helpers:Lang.S}}" Icon="{ui:SymbolIcon Add24}">
                        <ui:DropDownButton.Flyout>
                            <ContextMenu> 
                                <MenuItem Header="{Binding [Script_010_86c033], Source={x:Static helpers:Lang.S}}" Command="{Binding AddJsScriptCommand}" />
                                <MenuItem Header="{Binding [Script_011_7c48bd], Source={x:Static helpers:Lang.S}}" Command="{Binding AddPathingCommand}" />
                                <MenuItem Header="{Binding [Script_012_ea035d], Source={x:Static helpers:Lang.S}}" Command="{Binding AddKmScriptCommand}" />
                                <MenuItem Header="Shell" Command="{Binding AddShellCommand}" />
                            </ContextMenu>
                        </ui:DropDownButton.Flyout>
                    </ui:DropDownButton>
                    <Separator Width="10" Opacity="0" />
                    <ui:DropDownButton Content="{Binding [Btn_MoreFeatures], Source={x:Static helpers:Lang.S}}">
                        <ui:DropDownButton.Flyout>
                            <ContextMenu>
                                <MenuItem Header="{Binding [Script_013_288f0c], Source={x:Static helpers:Lang.S}}" Command="{Binding ClearTasksCommand}" />
                                <MenuItem Header="{Binding [Script_014_409074], Source={x:Static helpers:Lang.S}}" Command="{Binding OpenLogParseCommand}" />
                                <MenuItem Header="{Binding [Script_015_04a18a], Source={x:Static helpers:Lang.S}}" Command="{Binding OpenLocalScriptRepoCommand}" />
                                <MenuItem Header="{Binding [Script_016_d7f2f8], Source={x:Static helpers:Lang.S}}" Command="{Binding UpdateTasksCommand}" />
                                <MenuItem Header="{Binding [Script_017_593b3d], Source={x:Static helpers:Lang.S}}" Command="{Binding ReverseTaskOrderCommand}" />
                                <MenuItem Header="{Binding [Script_018_c36121], Source={x:Static helpers:Lang.S}}" Command="{Binding ExportMergerJsonsCommand}" />
                            </ContextMenu>
                        </ui:DropDownButton.Flyout>
                    </ui:DropDownButton>
                    <Separator Width="10" Opacity="0" />
                    <ui:Button Command="{Binding OpenScriptGroupSettingsCommand}"
                               Content="{Binding [Btn_Settings], Source={x:Static helpers:Lang.S}}"
                               Icon="{ui:SymbolIcon Settings24}" />
                    <!--<ui:TextBlock VerticalAlignment="Center"
                                  FontSize="16"
                                  Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                                  TextWrapping="Wrap">
                        停止运行请配置并使用快捷键！
                    </ui:TextBlock>-->
                </StackPanel>

                <Separator Grid.Row="3"
                           Height="8"
                           Opacity="0" />

                <Grid Grid.Row="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="20" />
                    </Grid.ColumnDefinitions>
                    <Grid x:Name="Col1" Grid.Column="0" />
                    <Grid x:Name="Col2" Grid.Column="1" />
                    <Grid x:Name="Col3" Grid.Column="2" />
                    <Grid x:Name="Col4" Grid.Column="3" />
                </Grid>
                <ui:ListView Grid.Row="5"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Stretch"
                             dd:DragDrop.IsDragSource="True"
                             dd:DragDrop.IsDropTarget="True"
                             dd:DragDrop.UseDefaultDragAdorner="True"
                             ItemsSource="{Binding SelectedScriptGroup.Projects, Mode=TwoWay}"
                             SelectionMode="Single">
     
                  
                    <ListView.View>

                        <GridView ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderDarkStyle}">
                            <GridViewColumn Width="{Binding ElementName=Col1, Path=ActualWidth}"
                                            DisplayMemberBinding="{Binding Index}"
                                            Header="#" />
                            <GridViewColumn Width="{Binding ElementName=Col2, Path=ActualWidth}"
                                  
                                            Header="{Binding [Script_019_d7ec2d], Source={x:Static helpers:Lang.S}}" >
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" FontSize="14" ToolTip="{Binding FolderName}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding NextFlag}" Value="True">
                                                            <Setter Property="Foreground" Value="Green" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>

                            </GridViewColumn>
                            <GridViewColumn Width="{Binding ElementName=Col3, Path=ActualWidth}"
                                            DisplayMemberBinding="{Binding TypeDesc}"
                                            Header="{Binding [Script_020_226b09], Source={x:Static helpers:Lang.S}}" />
                            <GridViewColumn Width="{Binding ElementName=Col4, Path=ActualWidth}" Header="{Binding [Script_021_a4fc48], Source={x:Static helpers:Lang.S}}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <ui:ToggleSwitch
                                            IsChecked="{Binding Status, Converter={StaticResource BooleanToEnableTextConverter}, Mode=TwoWay}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                    <ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Command="{Binding AddJsScriptCommand}" Header="{Binding [Script_022_9c8554], Source={x:Static helpers:Lang.S}}" />
                            <MenuItem Command="{Binding AddPathingCommand}" Header="{Binding [Script_023_dd011f], Source={x:Static helpers:Lang.S}}" />
                            <MenuItem Command="{Binding AddKmScriptCommand}" Header="{Binding [Script_024_f34b14], Source={x:Static helpers:Lang.S}}" />
                            <MenuItem Command="{Binding AddShellCommand}" Header="{Binding [Script_025_b7d598], Source={x:Static helpers:Lang.S}}" />
                            <MenuItem Command="{Binding AddNextFlagCommand}" Header="{Binding [Script_026_5a0124], Source={x:Static helpers:Lang.S}}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                      />
                            
                            <MenuItem Command="{Binding EditScriptCommonCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                      Header="{Binding [Script_027_15c5d7], Source={x:Static helpers:Lang.S}}" />
                            <MenuItem Command="{Binding EditJsScriptSettingsCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                      Header="{Binding [Script_028_830e41], Source={x:Static helpers:Lang.S}}">
                                <MenuItem.Style>
                                    <Style TargetType="MenuItem">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger
                                                Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem.Type}"
                                                Value="Javascript">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </MenuItem.Style>
                            </MenuItem>
                            <MenuItem Command="{Binding OpenScriptFolderCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                      Header="{Binding [Script_029_5218ae], Source={x:Static helpers:Lang.S}}">
                                <MenuItem.Style>
                                    <Style TargetType="MenuItem">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger
                                                Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem.Type}"
                                                Value="Shell">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </MenuItem.Style>
                            </MenuItem>
                            <MenuItem Command="{Binding DeleteScriptCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                      Header="{Binding [Script_030_86048b], Source={x:Static helpers:Lang.S}}" />
                            <MenuItem Command="{Binding DeleteScriptByFolderCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                      Header="{Binding [Script_031_3d6ed2], Source={x:Static helpers:Lang.S}}">
                                <MenuItem.Style>
                                    <Style TargetType="MenuItem">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger
                                                Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem.Type}"
                                                Value="Pathing">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </MenuItem.Style>
                            </MenuItem>
                        </ContextMenu>
                    </ListView.ContextMenu>
                    <ListView.Style>
                        <Style TargetType="{x:Type ListView}">
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Background" Value="Transparent" />
                        </Style>
                    </ListView.Style>
                </ui:ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>