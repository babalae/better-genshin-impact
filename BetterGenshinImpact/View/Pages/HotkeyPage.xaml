<Page x:Class="BetterGenshinImpact.View.Pages.HotKeyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:BetterGenshinImpact.View.Pages"
      xmlns:pages="clr-namespace:BetterGenshinImpact.ViewModel.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:models="clr-namespace:BetterGenshinImpact.Core.Config"
      xmlns:controls="clr-namespace:BetterGenshinImpact.View.Controls.HotKey"
      xmlns:src="BetterGenshinImpact.View.Controls.Hotkey"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=pages:HotKeyPageViewModel}"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      d:DesignHeight="850" d:DesignWidth="600"
      Title="HotkeyPage">

    <Grid Margin="42,16,42,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ui:TextBlock
            Grid.Row="0"
            Margin="0,0,0,8"
            FontTypography="BodyStrong"
            Text="快捷键设置" />

        <ListView Grid.Row="1" ItemsSource="{Binding HotkeyConfigs}">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type models:HotKeyConfig}">
                    <Grid Margin="8,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="180" />
                        </Grid.ColumnDefinitions>
                        <ui:TextBlock Grid.Column="0" Text="{Binding DisplayName}" VerticalAlignment="Center" />
                        <controls:HotKeyTextBox
                            Grid.Column="1" 
                            Hotkey="{Binding Hotkey}" 
                            TextAlignment="Center"
                            Style="{DynamicResource PlaceholderTextBox}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>