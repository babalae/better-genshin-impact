<ui:FluentWindow x:Class="BetterGenshinImpact.View.Windows.ImageEditWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:i18n="clr-namespace:BetterGenshinImpact.Service.I18n"
                 Title="{i18n:T 编辑背景图片}"
                 Width="920"
                 Height="680"
                 MinWidth="640"
                 MinHeight="480"
                 Background="#202020"
                 ExtendsContentIntoTitleBar="True"
                 FontFamily="{DynamicResource TextThemeFontFamily}"
                 WindowBackdropType="Auto"
                 WindowStartupLocation="CenterOwner"
                 mc:Ignorable="d">
    <!--
      背景图片编辑对话框：
      - 左右旋转 90°（TransformedBitmap 实现）
      - 鼠标拖拽绘制/移动/边缘缩放裁剪框（CroppedBitmap 实现）
      - 三种退出路径：保存并使用（编辑后副本）/ 使用原图 / 取消
    -->
    <Grid>
        <ui:Grid Margin="0,48,0,0" RowDefinitions="Auto,*,Auto">
            <!-- 工具栏 -->
            <StackPanel Grid.Row="0"
                        Margin="16,4,16,8"
                        HorizontalAlignment="Left"
                        Orientation="Horizontal">
                <ui:Button MinWidth="96"
                           Margin="0,0,8,0"
                           Click="RotateLeft_Click"
                           Content="{i18n:T ↺ 左转 90°}"
                           ToolTip="{i18n:T 逆时针旋转 90 度}" />
                <ui:Button MinWidth="96"
                           Margin="0,0,8,0"
                           Click="RotateRight_Click"
                           Content="{i18n:T ↻ 右转 90°}"
                           ToolTip="{i18n:T 顺时针旋转 90 度}" />
                <ui:Button MinWidth="96"
                           Margin="0,0,16,0"
                           Click="ResetCrop_Click"
                           Content="{i18n:T □ 重置裁剪}"
                           ToolTip="{i18n:T 清除裁剪区域，保留整张图片}" />
                <ui:TextBlock Name="InfoText"
                              Margin="8,0,0,0"
                              VerticalAlignment="Center"
                              FontSize="12"
                              Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                              Text="{i18n:T 在图片上拖拽可框选裁剪区域；拖动框内移动、贴边缩放}" />
            </StackPanel>

            <!-- 图片预览 + 裁剪框覆盖层 -->
            <Border Grid.Row="1"
                    Margin="16,0,16,8"
                    Background="#141414"
                    BorderBrush="#3A3A3A"
                    BorderThickness="1"
                    ClipToBounds="True">
                <Grid>
                    <Image Name="PreviewImage"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Stretch="Uniform" />
                    <!-- 透明 Canvas 负责捕获鼠标事件并绘制裁剪矩形 -->
                    <Canvas Name="CropCanvas"
                            Background="Transparent"
                            MouseLeftButtonDown="CropCanvas_MouseLeftButtonDown"
                            MouseLeftButtonUp="CropCanvas_MouseLeftButtonUp"
                            MouseMove="CropCanvas_MouseMove"
                            SizeChanged="CropCanvas_SizeChanged">
                        <Rectangle Name="CropRect"
                                   Canvas.Left="0"
                                   Canvas.Top="0"
                                   Fill="#224FC3F7"
                                   IsHitTestVisible="False"
                                   Stroke="#4FC3F7"
                                   StrokeDashArray="4 2"
                                   StrokeThickness="1.5"
                                   Visibility="Collapsed" />
                    </Canvas>
                </Grid>
            </Border>

            <!-- 底部操作区 -->
            <Grid Grid.Row="2" Margin="16,0,16,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="0"
                           Width="88"
                           Click="CancelButton_Click"
                           Content="{i18n:T 取消}" />
                <ui:Button Grid.Column="2"
                           Margin="0,0,8,0"
                           Width="110"
                           Click="UseOriginalButton_Click"
                           Content="{i18n:T 使用原图}"
                           ToolTip="{i18n:T 不做任何修改，直接使用选择的图片}" />
                <ui:Button Grid.Column="2"
                           Margin="0,0,130,0"
                           Width="120"
                           Appearance="Primary"
                           Click="SaveButton_Click"
                           Content="{i18n:T 保存并使用}"
                           ToolTip="{i18n:T 应用旋转与裁剪，另存为副本后使用}" />
            </Grid>
        </ui:Grid>
        <ui:TitleBar Title="{i18n:T 编辑背景图片}">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Resources/Images/logo.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
    </Grid>
</ui:FluentWindow>
