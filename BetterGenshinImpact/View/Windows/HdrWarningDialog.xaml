<ui:FluentWindow x:Class="BetterGenshinImpact.View.Windows.HdrWarningDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:behavior="clr-namespace:BetterGenshinImpact.View.Behavior"
                 Width="620"
                 Height="340"
                 MinWidth="520"
                 MinHeight="300"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 ExtendsContentIntoTitleBar="True"
                 behavior:AutoTranslateInterceptor.EnableAutoTranslate="True"
                 FontFamily="{DynamicResource TextThemeFontFamily}"
                 ResizeMode="NoResize"
                 WindowBackdropType="Auto"
                 WindowStartupLocation="CenterOwner"
                 WindowStyle="SingleBorderWindow"
                 mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                     ShowMaximize="False"
                     ShowMinimize="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Resources/Images/logo.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="24,16,24,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:SymbolIcon Grid.Column="0"
                           Margin="0,0,16,0"
                           VerticalAlignment="Top"
                           FontSize="44"
                           Foreground="{DynamicResource SystemFillColorAttentionBrush}"
                           Symbol="Warning24" />

            <ScrollViewer Grid.Column="1"
                          VerticalScrollBarVisibility="Auto">
                <ui:TextBlock x:Name="MessageTextBlock"
                              FontSize="14"
                              TextAlignment="Left"
                              TextWrapping="Wrap"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            </ScrollViewer>
        </Grid>

        <StackPanel Grid.Row="2"
                    Margin="24,0,24,24"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <ui:Button x:Name="SwitchButton"
                       MinWidth="118"
                       Margin="0,0,8,0"
                       Appearance="Primary"
                       Click="SwitchButton_Click"
                       Content="切到 HDR 截图"
                       ToolTip="切换到 WindowsGraphicsCapture（HDR）" />
            <ui:Button x:Name="SettingsButton"
                       MinWidth="108"
                       Margin="0,0,8,0"
                       Appearance="Secondary"
                       Click="SettingsButton_Click"
                       Content="图形设置"
                       ToolTip="打开 Windows 图形设置" />
            <ui:Button x:Name="ContinueButton"
                       MinWidth="92"
                       Appearance="Secondary"
                       Click="ContinueButton_Click"
                       Content="继续执行" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
