<ui:FluentWindow x:Class="BetterGenshinImpact.View.Windows.GuardianAvatarListDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:i18n="clr-namespace:BetterGenshinImpact.Service.I18n"
                 Title="{i18n:T 盾奶位名单}"
                 Width="640"
                 Height="560"
                 d:Background="#D2D2D2"
                 ExtendsContentIntoTitleBar="True"
                 FontFamily="{DynamicResource TextThemeFontFamily}"
                 Tag="GuardianAvatarListDialog"
                 WindowBackdropType="Mica"
                 WindowStartupLocation="CenterOwner"
                 mc:Ignorable="d">
    <Grid>
        <Grid Margin="8,48,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ui:TextBlock Grid.Row="0"
                          Margin="4,0,4,8"
                          Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                          Text="{i18n:T 每行一个盾奶位角色（自动模式按行顺序匹配，越靠前越优先）。需要长按E的角色在名字后加“长按”，如：钟离长按。可直接修改保存。}"
                          TextWrapping="Wrap" />

            <ui:TextBox Grid.Row="1"
                        x:Name="ListTextBox"
                        AcceptsReturn="True"
                        AcceptsTab="True"
                        HorizontalScrollBarVisibility="Auto"
                        VerticalScrollBarVisibility="Auto"
                        TextWrapping="NoWrap"
                        FontFamily="Cascadia Mono,Consolas,Microsoft YaHei UI"
                        FontSize="13"
                        Text="{Binding ListText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <Grid Grid.Row="2" Margin="0,10,0,8">
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button MinWidth="90"
                            Margin="8,0,8,0"
                            Command="{Binding RestoreDefaultCommand}"
                            Content="{i18n:T 恢复默认}" />
                    <Button MinWidth="90"
                            Margin="8,0,8,0"
                            Command="{Binding SaveCommand}"
                            Content="{i18n:T 保存}" />
                    <Button MinWidth="90"
                            Command="{Binding CloseCommand}"
                            Content="{i18n:T 关闭}" />
                </StackPanel>
            </Grid>
        </Grid>

        <ui:TitleBar Title="{i18n:T 盾奶位名单}">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Resources/Images/logo.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
    </Grid>
</ui:FluentWindow>
