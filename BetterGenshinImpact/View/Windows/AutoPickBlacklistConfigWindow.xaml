<ui:FluentWindow x:Class="BetterGenshinImpact.View.Windows.AutoPickBlacklistConfigWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:i18n="clr-namespace:BetterGenshinImpact.Service.I18n"
                 Width="820"
                 Height="640"
                 MinWidth="680"
                 MinHeight="520"
                 Title="{i18n:T 黑名单模式配置}"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Auto"
                 WindowStartupLocation="CenterOwner"
                 FontFamily="{DynamicResource TextThemeFontFamily}"
                 mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{i18n:T 黑名单模式配置}">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Filter24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Border Grid.Row="1"
                Margin="20,12,20,12"
                Padding="14"
                Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8">
            <StackPanel>
                <TextBlock FontWeight="SemiBold"
                           Text="{i18n:T 默认拾取未被排除的内容}" />
                <TextBlock Margin="0,5,0,0"
                           Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                           Text="{i18n:T “拾取”规则优先级最高，可放行聊天/设置图标和“不拾取”规则中的同名内容。每行填写一条完整的 OCR 文本。}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <TabControl Grid.Row="2" Margin="20,0,20,0">
            <TabItem Header="{i18n:T 不拾取（主要规则）}">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Margin="0,0,0,12"
                               Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                               Text="{i18n:T 黑名单模式启用期间，默认、精确和模糊不拾取规则始终生效。}"
                               TextWrapping="Wrap" />

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Margin="0,0,0,6"
                                       FontWeight="SemiBold"
                                       Text="{i18n:T 精确匹配}" />
                             <ui:TextBox Grid.Row="1"
                                         AcceptsReturn="True"
                                         AcceptsTab="False"
                                         PlaceholderText="{i18n:T 每行一条记录，例如：精致的宝箱}"
                                        Text="{Binding DoNotPickText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        TextWrapping="NoWrap"
                                        VerticalContentAlignment="Top"
                                        VerticalScrollBarVisibility="Auto"
                                        HorizontalScrollBarVisibility="Auto" />
                        </Grid>
                        <Grid Grid.Column="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Margin="0,0,0,6"
                                       FontWeight="SemiBold"
                                       Text="{i18n:T 模糊匹配}" />
                             <ui:TextBox Grid.Row="1"
                                         AcceptsReturn="True"
                                         AcceptsTab="False"
                                         PlaceholderText="{i18n:T 每行一个关键词，OCR 文本包含该关键词时不拾取}"
                                        Text="{Binding FuzzyDoNotPickText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        TextWrapping="NoWrap"
                                        VerticalContentAlignment="Top"
                                        VerticalScrollBarVisibility="Auto"
                                        HorizontalScrollBarVisibility="Auto" />
                        </Grid>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="{i18n:T 拾取（优先放行）}">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold" Text="{i18n:T 启用拾取规则}" />
                            <TextBlock Margin="0,3,12,0"
                                       Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                       Text="{i18n:T 精确命中后直接拾取，并优先于图标排除和不拾取规则。}"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1"
                                         VerticalAlignment="Center"
                                         IsChecked="{Binding PickEnabled, Mode=TwoWay}" />
                    </Grid>
                    <ui:TextBox Grid.Row="1"
                                AcceptsReturn="True"
                                AcceptsTab="False"
                                IsEnabled="{Binding PickEnabled}"
                                PlaceholderText="{i18n:T 每行一条记录，例如：调查}"
                                Text="{Binding PickText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                TextWrapping="NoWrap"
                                VerticalContentAlignment="Top"
                                VerticalScrollBarVisibility="Auto"
                                HorizontalScrollBarVisibility="Auto" />
                </Grid>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="3" Margin="20,16,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center"
                       Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                       Text="{i18n:T 保存后立即刷新自动拾取规则。}" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button MinWidth="88"
                           Margin="0,0,10,0"
                           Command="{Binding CancelCommand}"
                           Content="{i18n:T 取消}"
                           IsCancel="True" />
                <ui:Button MinWidth="88"
                           Appearance="Primary"
                           Command="{Binding SaveCommand}"
                           Content="{i18n:T 保存}"
                           IsDefault="True" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
