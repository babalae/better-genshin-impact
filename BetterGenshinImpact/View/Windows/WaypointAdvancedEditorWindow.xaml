<ui:FluentWindow x:Class="BetterGenshinImpact.View.Windows.WaypointAdvancedEditorWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:windows="clr-namespace:BetterGenshinImpact.ViewModel.Windows"
                 Title="高级参数"
                 Width="620"
                 Height="430"
                 MinWidth="520"
                 MinHeight="360"
                 d:DataContext="{d:DesignInstance Type=windows:RecordedWaypointViewModel}"
                 ExtendsContentIntoTitleBar="True"
                 ResizeMode="CanResize"
                 WindowBackdropType="Mica"
                 WindowStartupLocation="CenterOwner"
                 WindowStyle="SingleBorderWindow"
                 mc:Ignorable="d">
    <Grid Background="{DynamicResource ApplicationBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="高级参数" />

        <Border Grid.Row="1"
                Margin="16"
                Padding="16"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="84" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.ColumnSpan="2"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Text="{Binding Index, StringFormat=第 {0} 个路线点}" />

                <TextBlock Grid.Row="1"
                           Margin="0,16,12,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Text="描述" />
                <ui:TextBox Grid.Row="1"
                            Grid.Column="1"
                            Margin="0,16,0,0"
                            MinHeight="34"
                            Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="2"
                           Margin="0,10,12,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Text="怪物标签" />
                <ui:TextBox Grid.Row="2"
                            Grid.Column="1"
                            Margin="0,10,0,0"
                            MinHeight="34"
                            Text="{Binding MonsterTag, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="3"
                           Margin="0,10,12,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Text="误识别" />
                <ui:TextBox Grid.Row="3"
                            Grid.Column="1"
                            Margin="0,10,0,0"
                            MinHeight="34"
                            Text="{Binding MisidentificationTypesText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Grid Grid.Row="4"
                      Grid.Column="1"
                      Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="92" />
                        <ColumnDefinition Width="120" />
                    </Grid.ColumnDefinitions>
                    <ui:TextBox MinHeight="34"
                                PlaceholderText="处理方式"
                                Text="{Binding MisidentificationHandlingMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <ui:NumberBox Grid.Column="1"
                                  Margin="10,0,0,0"
                                  Minimum="0"
                                  SmallChange="100"
                                  Value="{Binding MisidentificationArrivalTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <CheckBox Grid.Column="2"
                              Margin="14,0,0,0"
                              VerticalAlignment="Center"
                              Content="区分拾取"
                              IsChecked="{Binding EnableMonsterLootSplit, Mode=TwoWay}" />
                </Grid>

                <ui:Button Grid.Row="5"
                           Grid.ColumnSpan="2"
                           Width="96"
                           Height="34"
                           Margin="0,18,0,0"
                           HorizontalAlignment="Right"
                           Click="CloseButton_Click"
                           Content="完成"
                           Icon="{ui:SymbolIcon Checkmark24}" />
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
