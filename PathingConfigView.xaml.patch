To localize the PathingConfigView.xaml file, the following changes need to be made:

1. Add the markup namespace to the XAML header:
```xml
xmlns:markup="clr-namespace:BetterGenshinImpact.Markup"
```

2. Replace all hardcoded Chinese strings with LocalizeExtension:

Original:
```xml
<ui:TextBlock Margin="0,0,0,8"
              FontSize="16"
              FontTypography="BodyStrong"
              Text="地图追踪设置（相关配置优先级从高到低,UI样式还有问题未解决）" />
```

Replace with:
```xml
<ui:TextBlock Margin="0,0,0,8"
              FontSize="16"
              FontTypography="BodyStrong"
              Text="{markup:Localize Key=pathing.settings}" />
```

Original:
```xml
<ui:TextBlock Margin="0,0,0,8"
              FontTypography="BodyStrong"
              Text="队伍切换设置（路径中存在符合条件的动作、采集物的情况下，自动切换至对应队伍）" />
```

Replace with:
```xml
<ui:TextBlock Margin="0,0,0,8"
              FontTypography="BodyStrong"
              Text="{markup:Localize Key=pathing.teamSwitchSettings}" />
```

Original:
```xml
<ui:TextBlock Margin="10,0"
              VerticalAlignment="Center"
              Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}">
    则切换到队伍
</ui:TextBlock>
```

Replace with:
```xml
<ui:TextBlock Margin="10,0"
              VerticalAlignment="Center"
              Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
              Text="{markup:Localize Key=pathing.switchToTeam}" />
```

Original:
```xml
<Button
    Command="{Binding DataContext.RemovePartyConditionConfigCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
    CommandParameter="{Binding}"
    Content="删除" />
```

Replace with:
```xml
<Button
    Command="{Binding DataContext.RemovePartyConditionConfigCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
    CommandParameter="{Binding}"
    Content="{markup:Localize Key=pathing.delete}" />
```

Original:
```xml
<Button HorizontalAlignment="Left"
        Command="{Binding AddPartyConditionConfigCommand}"
        Content="+ 添加条件" />
```

Replace with:
```xml
<Button HorizontalAlignment="Left"
        Command="{Binding AddPartyConditionConfigCommand}"
        Content="{markup:Localize Key=pathing.addCondition}" />
```

Original:
```xml
<ui:TextBlock Margin="0,0,0,8"
              FontTypography="BodyStrong"
              Text="角色设置（存在多个符合条件的角色情况下，优先使用游戏内编队编号更小的）" />
```

Replace with:
```xml
<ui:TextBlock Margin="0,0,0,8"
              FontTypography="BodyStrong"
              Text="{markup:Localize Key=pathing.characterSettings}" />
```

Original:
```xml
<ui:TextBlock Margin="10,0"
              VerticalAlignment="Center"
              Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}">
    则角色执行
</ui:TextBlock>
```

Replace with:
```xml
<ui:TextBlock Margin="10,0"
              VerticalAlignment="Center"
              Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
              Text="{markup:Localize Key=pathing.characterExecute}" />
```

Original:
```xml
<Button
    Command="{Binding DataContext.RemoveAvatarConditionConfigCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
    CommandParameter="{Binding}"
    Content="删除" />
```

Replace with:
```xml
<Button
    Command="{Binding DataContext.RemoveAvatarConditionConfigCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
    CommandParameter="{Binding}"
    Content="{markup:Localize Key=pathing.delete}" />
```

Original:
```xml
<Button HorizontalAlignment="Left"
        Command="{Binding AddAvatarConditionConfigCommand}"
        Content="+ 添加条件" />
```

Replace with:
```xml
<Button HorizontalAlignment="Left"
        Command="{Binding AddAvatarConditionConfigCommand}"
        Content="{markup:Localize Key=pathing.addCondition}" />
```

Original:
```xml
<ui:TextBlock Grid.Row="0"
              Grid.Column="0"
              FontTypography="Body"
              Text="使用小道具的间隔时间（毫秒）"
              TextWrapping="Wrap" />
<ui:TextBlock Grid.Row="1"
              Grid.Column="0"
              Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
              Text="在移动期间自动使用小道具，填0为不使用"
              TextWrapping="Wrap" />
```

Replace with:
```xml
<ui:TextBlock Grid.Row="0"
              Grid.Column="0"
              FontTypography="Body"
              Text="{markup:Localize Key=pathing.gadgetIntervalMs}"
              TextWrapping="Wrap" />
<ui:TextBlock Grid.Row="1"
              Grid.Column="0"
              Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
              Text="{markup:Localize Key=pathing.gadgetIntervalDescription}"
              TextWrapping="Wrap" />
```

Original:
```xml
<ui:TextBlock Grid.Row="0"
              Grid.Column="0"
              FontTypography="Body"
              Text="只在传送点时恢复"
              TextWrapping="Wrap" />
<ui:TextBlock Grid.Row="1"
              Grid.Column="0"
              Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
              Text="只有路径在传送点时，才会去七天神像恢复"
              TextWrapping="Wrap" />
```

Replace with:
```xml
<ui:TextBlock Grid.Row="0"
              Grid.Column="0"
              FontTypography="Body"
              Text="{markup:Localize Key=pathing.onlyInTeleportRecover}"
              TextWrapping="Wrap" />
<ui:TextBlock Grid.Row="1"
              Grid.Column="0"
              Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
              Text="{markup:Localize Key=pathing.onlyInTeleportRecoverDescription}"
              TextWrapping="Wrap" />
```

3. Add the following translation keys to both language files:

For en-US.json:
```json
"pathing.settings": "Map Tracking Settings",
"pathing.teamSwitchSettings": "Team Switch Settings",
"pathing.teamSwitchDescription": "Automatically switch to the corresponding team when actions or collectibles matching conditions exist in the path",
"pathing.characterSettings": "Character Settings",
"pathing.characterSettingsDescription": "When multiple characters meet the conditions, priority is given to the character with a smaller team number in the game",
"pathing.gadgetIntervalMs": "Gadget Use Interval (milliseconds)",
"pathing.gadgetIntervalDescription": "Automatically use gadgets while moving, enter 0 to disable",
"pathing.onlyInTeleportRecover": "Only Recover at Teleport Points",
"pathing.onlyInTeleportRecoverDescription": "Only go to Statues of The Seven for recovery when the path is at a teleport point",
"pathing.switchToTeam": "then switch to team",
"pathing.characterExecute": "then character executes",
"pathing.delete": "Delete",
"pathing.addCondition": "+ Add Condition"
```

For zh-CN.json:
```json
"pathing.settings": "地图追踪设置",
"pathing.teamSwitchSettings": "队伍切换设置",
"pathing.teamSwitchDescription": "路径中存在符合条件的动作、采集物的情况下，自动切换至对应队伍",
"pathing.characterSettings": "角色设置",
"pathing.characterSettingsDescription": "存在多个符合条件的角色情况下，优先使用游戏内编队编号更小的",
"pathing.gadgetIntervalMs": "使用小道具的间隔时间（毫秒）",
"pathing.gadgetIntervalDescription": "在移动期间自动使用小道具，填0为不使用",
"pathing.onlyInTeleportRecover": "只在传送点时恢复",
"pathing.onlyInTeleportRecoverDescription": "只有路径在传送点时，才会去七天神像恢复",
"pathing.switchToTeam": "则切换到队伍",
"pathing.characterExecute": "则角色执行",
"pathing.delete": "删除",
"pathing.addCondition": "+ 添加条件"
```